{
  "name": "deposit-withdraw-status",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build --workspace @status-monitor/shared && npm run dev --workspace @status-monitor/api & npm run dev --workspace @status-monitor/web",
    "build": "npm run build --workspace @status-monitor/shared && npm run build --workspace @status-monitor/api && npm run build --workspace @status-monitor/web",
    "test": "npm run test --workspaces",
    "test:api": "npm run test --workspace @status-monitor/api --",
    "test:web": "npm run test --workspace @status-monitor/web --",
    "start:api": "npm run start --workspace @status-monitor/api",
    "pm2:start": "pm2 start ecosystem.config.cjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "pm2": "^5.4.3"
  }
}
